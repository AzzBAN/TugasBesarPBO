package Object;

import java.time.LocalDate;

public class admin extends user {
    public admin(String nama, String username, String password, String posisi, String tglLahir, int id) {
        super(nama, username, password, posisi, tglLahir, id);
    }
}
